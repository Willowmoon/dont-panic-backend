const express = require('express')
const router = express.Router()

// Require the route from controller: const cbController = require('../controllers/cookbooks');
// Write the route set up: router.get('/', cbController.getAllCookbooks);

module.exports = router;