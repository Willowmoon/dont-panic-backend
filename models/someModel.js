//import connection
const mongoose = require('../db/connection');
/* Create schema
    Properties:
    title (string),
    yearPublished (integer),

    const CookbookSchema = mongoose.Schema({
    title: String,
    yearPublished: Number
});
*/


// Create model: const Cookbook = mongoose.model("Cookbook", CookbookSchema)
//export model: module.exports = Cookbook;